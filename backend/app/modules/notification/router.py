"""notification/router.py - Notification HTTP API placeholder.

Intentional stub:
- Notification provider/service/logging is active.
- Public HTTP endpoints are intentionally not exposed yet.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/notifications", tags=["notifications"])
